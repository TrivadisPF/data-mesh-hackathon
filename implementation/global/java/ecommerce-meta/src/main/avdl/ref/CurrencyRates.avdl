@namespace("com.trivadis.ecommerce.ref.avro")
protocol CurrencyRatesProtocol {

	@domain("reference")
	record CurrencyRates {
		timestamp_ms timestamp;
		string baseCurrencyIsoCode;
		map<double> rate;
	}
}