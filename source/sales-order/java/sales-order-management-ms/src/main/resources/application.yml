server:
  port: 8082

logging:
  level:
    root: INFO

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://10.0.0.72:5432/postgres
    username: postgres
    password: abc123!
    driverClassName: org.postgresql.Driver

